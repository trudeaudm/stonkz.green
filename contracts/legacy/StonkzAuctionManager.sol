// SPDX-License-Identifier: BUSL-1.1
pragma solidity ^0.8.26;

/// @title StonkzAuctionManager (SKELETON)
/// @notice Factory + creator bonds + run-away protocol (spec §7) + guarded-launch
///         caps: per-auction raise cap (~$50k) and global TVL cap, raised
///         stepwise behind a timelock; caps removed publicly at tier-1 audit.
contract StonkzAuctionManager {
    constructor() { revert("TODO"); }
}
